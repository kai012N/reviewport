# Changesets

This folder is managed by [Changesets](https://github.com/changesets/changesets).

When you make a user-facing change, run `npx changeset`, choose the bump level, and
write a short summary. The generated markdown file goes here and is consumed at
release time to update the version and `CHANGELOG.md`.

See [CONTRIBUTING.md](../CONTRIBUTING.md#changesets).
