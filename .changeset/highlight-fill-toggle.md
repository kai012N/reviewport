---
"reviewport": minor
---

Add a **Fill** toggle to the review panel header. The green highlight that marks a change tints the element, which can slightly cover the content you're verifying — turning Fill off keeps just the outline (so you still see *where* the change is) while leaving the changed content fully readable. The preference is remembered per session in `localStorage`.
